query Input {
  fulfillmentGroups {
    handle
    inventoryLocationHandles
    lines {
      id
      attribute(key: "_locationId") {
        key
        value
      }
    }
  }
  locations {
    handle
    id
  }
}
